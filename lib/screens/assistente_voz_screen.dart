import 'package:flutter/material.dart';

class VoiceAssistScreen extends StatelessWidget {
  const VoiceAssistScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('VoiceAssistScreen')),
      body: const Center(child: Text('VoiceAssistScreen Content')),
    );
  }
}
